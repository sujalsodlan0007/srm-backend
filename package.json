{
  "name": "sachin-chaudhary-backend",
  "version": "1.0.0",
  "description": "Lead capture backend for SRM Global Hub",
  "main": "dist/server.js",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/server.js",
    "dev": "npm run build && node dist/server.js",
    "test": "npm run build && node --test tests/**/*.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "express-rate-limit": "^7.1.0",
    "helmet": "^8.0.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^9.8.0",
    "nodemailer": "^7.0.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.1",
    "@types/cors": "^2.8.0",
    "@types/express": "^5.0.0",
    "@types/jsonwebtoken": "^9.0.1",
    "@types/node": "^22.10.1",
    "mongodb-memory-server": "^10.0.0",
    "typescript": "^5.5.3"
  }
}
